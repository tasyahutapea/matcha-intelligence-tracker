datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Matcha {
  id        Int      @id @default(autoincrement())
  name      String
  rating    Int
  notes     String?
  createdAt DateTime @default(now())
}